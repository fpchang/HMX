<template>
	<view class="operation">
		<view class="card">
			<view class="title"><text class="txt">功能菜单</text></view>
			<view class="list">
				
				<view class="list-item" v-for="item in menuList" >
					<navigator :url="item.url" hover-class="none">
					<view class="groupIcon">
						<image :src="item.icon" class="icon-style" mode="aspectFit"></image>
					</view>
					<view>
						<text class="label">{{item.label}}</text>
					</view>
					</navigator>
				</view>

				<view class="list-item" style="height:0"></view>
				<view class="list-item" style="height:0"></view>
				<view class="list-item" style="height:0"></view>
				<view class="list-item" style="height:0"></view>
			</view>
		</view>
	</view>
</template>

<script setup>
	type menu = {
		icon : string,
		label : string,
		bgColor:string,
		url : string
	}
	const menuList : Array<menu> = [{
		icon: "/static/icons/mine/set.png",
		label: "系统设置",
		bgColor:"#165dff1a",
		url: "/pages/set/set"
	},
	{
		icon: "/static/icons/mine/fedback.png",
		label: "意见反馈",
		bgColor:"#722ed11a",
		url: "/pages/mine/feedback/feedback"
	},
	{
		icon: "/static/icons/mine/kf.png",//rgb(250 173 20 / var(--tw-text-opacity, 1))
		label: "客服中心",
		bgColor:"#faad141a",
		url: "/pages/order/createOrder/createOrder"
	}
	]
</script>

<style lang="scss">
	.card {
		// background-color: #fff;
		padding: 15px;

		.title {
			padding: 5px 5px 15px 5px;

			.txt {
				color: #1f2937;
				font-weight: 700;
				color: #1f2937;
			}
		}

		.list {
			display: flex;
			flex-direction: row;
			flex-wrap: wrap;
			

			.list-item {
				width: 85px;
				height:100px;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;

				.groupIcon {
	
					display: flex;
					flex-direction: row;
					align-items: center;
					justify-content: center;
					
					
					

					.icon-style {
						width: 40px;
						height: 40px;
					}
				}

				.label {
					font-size: 13px;
					padding-top: 8px;
					color: #64748b;
				}
			}
		}
	}
</style>